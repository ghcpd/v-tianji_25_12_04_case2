// Re-export the refactored, canonical implementation so the surface API is
// consistent across the codebase. The refactored module contains error
// handling, adapters, and the canonical `User` type.
export * from './userService.refactored';

